// Example: Alert on form submit (optional, for demo)
document.querySelector('form')?.addEventListener('submit', function(e) {
    // e.preventDefault(); // Uncomment if testing without real submit
    alert('Message sent! (This is a demo alert)');
});